export enum ContractType {
    PROBATION = 0,
    PERMANENT = 1,
    TEMPORARY = 2,
    FREELANCER = 3,
}

export enum ContractStatus {
    NEW = 0,
    CONFIRMED = 1,
    TERMINATED = 2,
    CANCELED = 3,
}
